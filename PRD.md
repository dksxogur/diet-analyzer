# [PRD] 한 페이지 식단 칼로리 & 영양 균형 추정기 (Single-Page App)

---

## 1. 목표와 성공조건

### 1.1 목표
* 단일 화면 내에서 신체 정보(키, 몸무게), 운동/식단 목적, 음식 정보(텍스트 또는 사진)를 입력하면 즉시 하루 권장 대비 총 칼로리 및 탄·단·지 영양 균형을 분석하여 시각적 결과 카드 및 공유 기능을 제공한다.
* 백엔드 DB, 로그인, 결제 모듈 없이 클라이언트 사이드 중심(LLM API 직접 호출 구조)으로 동작하도록 구현한다.

### 1.2 성공 조건
* 사용자가 입력 완료 후 [분석하기] 버튼 클릭 시 **3초 이내**에 영양 평가 결과 화면이 출력된다.
* 생성된 요약 결과 카드를 **1클릭만으로 클립보드에 이미지로 복사**하거나 **브라우저 기본 공유(Web Share API) 창을 호출**할 수 있어야 한다.

---

## 2. 사용자 스토리
> **사용자로서**, 복잡한 로그인이나 가입 없이 키, 몸무게, 목표, 섭취한 음식(텍스트/사진)만 입력하여 하루 권장량 대비 영양 균형 정보를 빠르게 확인하고, 결과를 시각적 카드로 만들어 외부에 공유하고 싶다.

---

## 3. 화면 명세 (단일 화면 Layout)

화면은 스크롤 가능한 **단일 페이지(Single-Page Layout)**로 구성되며, 영역은 상단 **[입력 폼 영역]**과 하단 **[결과 & 공유 카드 영역]**으로 나뉜다.

### 3.1 입력 폼 영역 (Form Section)
1. **신체 정보 입력 영역**
   * 키 (`Height`): 숫자 입력 필드 (단위: cm)
   * 몸무게 (`Weight`): 숫자 입력 필드 (단위: kg)
2. **식단 목적 선택 영역 (`Goal`)**
   * 라디오 버튼 또는 세그먼트 컨트롤: `다이어트(체중 감량)` / `벌크업(근성장)` / `건강(유지)` 중 택 1 (기본값: 다이어트)
3. **음식 정보 입력 영역**
   * **텍스트 입력 영역**: `Textarea` 필드 (placeholder: "예: 제육볶음 1인분, 공기밥 1공기, 찌개 약간")
     * Textarea 하단 우측: 현재 글자 수 / 최대 글자 수 표시 (`(0 / 200)`)
   * **사진 업로드 영역**: 이미지 첨부 버튼 및 미리보기/삭제 기능 제공 (`.jpg`, `.png`, `.webp` 지원)
4. **분석 실행 버튼**
   * `[영양 분석하기]` 버튼 (Primary Button)

### 3.2 결과 & 공유 카드 영역 (Result & Share Section)
> *최초 접근 시 비활성화(Hidden) 상태이며, 분석 완료 시 화면에 노출됨.*

1. **영양 분석 요약 패널**
   * 총 추정 칼로리 (kcal) 및 하루 권장 칼로리 대비 비율 (%)
   * 탄수화물 / 단백질 / 지방 섭취량(g) 및 권장 비율 대비 상태 그래프/바
   * 과다/부족 영양소 경고 메세지 영역 (예: "⚠️ 단백질이 권장량 대비 부족합니다.")
2. **시각적 요약 공유 카드 (Image Card)**
   * 요약 정보(목적, 칼로리, 탄단지 비중, 평가 문구)가 깔끔하게 디자인된 렌더링 카드 컴포넌트
3. **공유 액션 버튼 모음**
   * `[카드 이미지 클립보드 복사]` 버튼
   * `[공유하기 (SNS/오픈채팅)]` 버튼
   * `[이미지 다운로드]` 버튼

---

## 4. 기능 명세

### 4.1 일일 권장 영양소 계산 (클라이언트 계산)
* 입력된 키, 몸무게, 목적 값을 기반으로 표준 BMR/TDEE 공식을 적용하여 하루 권장 칼로리 및 탄단지 권장 비율(g)을 기초 산출한다.

### 4.2 식단 분석 (LLM API 연동)
* 사용자가 입력한 텍스트 또는 이미지 데이터(Base64 변환)를 AI API로 전달한다.
* AI는 음식 목록을 파싱하여 **총 칼로리, 탄수화물(g), 단백질(g), 지방(g), 1줄 종합 평가 및 주의사항**을 JSON 규격으로 반환한다.

### 4.3 시각적 결과 카드 생성 및 공유
* 화면 하단의 결과 카드 DOM 요소를 이미지(PNG) 데이터로 변환한다.
* **1클릭 복사**: 변환된 이미지 Blob을 `navigator.clipboard.write()` API를 통해 클립보드에 복사한다.
* **1클릭 공유**: `navigator.share()` (Web Share API)를 호출하여 모바일/데스크톱의 시스템 공유 창(카카오톡, 메시지, 타 앱 등)을 띄운다. Web Share 미지원 환경에서는 자동으로 클립보드 복사 또는 이미지 다운로드 fallback을 실행한다.

### 4.4 명시적 안 만들 것 (Out of Scope)
1. **회원가입/로그인/소셜 연동**: 전체 제외.
2. **PG사 결제 및 구독 플랜**: 전체 제외.
3. **자체 DB 및 백엔드 서버**: 과거 기록 저장 기능 전체 제외.
4. **자체 AI 모델 학습**: 기구축된 외부 LLM API 프롬프트 연동으로 대체.
5. **개별 식단/레시피 추천**: 영양 분석 및 경고까지만 제공, 추천 알고리즘 제외.
6. **푸시 알림 및 리마인더**: 전체 제외.
7. **인앱 커뮤니티 및 카카오/인스타 SDK 연동**: Web Share API, 클립보드 복사, 이미지 파일 다운로드로 대체.

---

## 5. 예외 처리 (Exception Handling)

### 5.1 [입력 예외] 필수 입력값 누락 시
* **상황**: 키, 몸무게, 목적을 입력하지 않거나, 식단 정보(텍스트와 사진 모두)가 빈 상태에서 `[영양 분석하기]` 버튼을 누른 경우.
* **동작**:
  * 분석 요청을 진행하지 않는다.
  * 결과를 표시하지 않는다.
  * 누락된 입력 필드 하단에 안내 문구(예: "키와 몸무게를 입력해 주세요", "음식 텍스트 또는 사진을 추가해 주세요")를 **빨간색 텍스트(`#FF0000`)**로 표시하고 해당 입력창으로 포커싱한다.

### 5.2 [입력 예외] 글자 수 초과 및 글자 수 표시
* **상황**: 식단 텍스트 입력창 작성 시.
* **동작**:
  * Textarea 하단에 **`(현재 글자 수 / 최대 글자 수)`** (예: `(45 / 200)`) 형태의 인디케이터를 상시 표시한다.
  * 글자 수가 최대 글자 수(200자)를 초과하거나 다가갈 경우(예: 200자 도달 시) 인디케이터 텍스트 색상을 **빨간색(`#FF0000`)**으로 변경한다.
  * 200자를 초과하는 키보드 입력은 즉시 제한(Prevent Input)한다.

### 5.3 [네트워크/API 예외] AI 응답 실패 시 자동 재시도
* **상황**: 외부 AI API 타임아웃, 네트워크 끊김, 5xx 서버 에러 발생 시.
* **동작**:
  * UI에 **"AI 응답 실패"** 문구를 즉시 노출한다.
  * 1초 후 자동으로 **"AI 재분석 중..."** 문구로 변경하며 최대 1회 재요청을 시도한다.
  * 재시도까지 최종 실패 시, "네트워크 연결이 불안정합니다. 잠시 후 다시 시도해 주세요." 알림창을 띄운다.

### 5.4 [대기 예외] AI 응답 지연 시 로딩 처리
* **상황**: [영양 분석하기] 버튼 클릭 후 AI 응답을 기다리는 동안.
* **동작**:
  * 분석 버튼을 비활성화(Disabled) 처리하고 오버레이 스피너를 노출한다.
  * 화면 중앙에 **"AI 분석 중..."** 텍스트를 띄워 사용자가 진행 상태임을 인지하도록 한다.

### 5.5 [데이터 예외] 비정상/비논리적 결과 반환 시
* **상황**: AI 응답 결과의 값이 비정상적이거나 파싱 실패인 경우 (예: 칼로리가 음수로 반환, 텍스트가 식단과 전혀 상관없는 내용이라 영양 정보가 0으로 파싱됨, JSON Format Mismatch 등).
* **동작**:
  * 분석 결과 영역에 **"식단 입력값을 분석할 수 없습니다. 입력값(텍스트/사진)을 재점검해 주세요."** 문구를 노출한다.
  * 하단에 `[다시 시도하기]` 또는 `[결과 다시 분석하기]` 버튼을 노출하여 입력값을 수정 후 버튼을 다시 누르도록 유도한다.

### 5.6 [공유 예외] Web Share API 미지원 브라우저 대응
* **상황**: 브라우저/환경이 `navigator.share` 또는 `navigator.clipboard`를 지원하지 않는 경우.
* **동작**:
  * 공유 버튼 클릭 시 "이 브라우저에서는 공유 기능을 지원하지 않습니다. 이미지를 직접 저장합니다." 안내 팝업을 띄운 뒤, 생성된 결과 카드 이미지를 자동으로 파일(`.png`)로 다운로드 처리한다.

---

## 6. 완료 조건 (Definition of Done)

1. **단일 화면 구성 완료**: 별도의 페이지 이동 없이 입력, 분석, 카드 생성, 공유가 한 화면 내에서 완료되는가?
2. **핵심 분석 출력**: 키, 몸무게, 목적, 식단 정보 입력 시 총 칼로리, 탄단지 영양소, 부족/과다 경고 문구가 정상 표시되는가?
3. **요약 결과 카드 & 공유 동작**: 결과 카드가 시각적 이미지로 변환되어 클립보드 복사 또는 공유 창 호출이 1클릭으로 실행되는가?
4. **예외 처리 5종 이상 검증 완료**:
   * [ ] 빈 입력 시 빨간색 경고 문구 표시
   * [ ] 글자 수 인디케이터 동작 및 초과 시 빨간색 변경
   * [ ] 응답 지연 시 "AI 분석 중..." 노출
   * [ ] AI 응답 실패 시 "AI 응답 실패" 후 "AI 재분석 중" 자동 처리
   * [ ] 이상 결과 시 입력값 재점검 유도 안내 및 재시도 유도
5. **Scope 준수**: DB, 로그인, 결제, 백엔드 서버 구축 없이 순수 클라이언트 단일 앱 스펙을 만족하는가?